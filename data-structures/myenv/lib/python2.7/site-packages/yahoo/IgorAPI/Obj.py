class Obj(object):
	
	def __init__(self, dic):
		self.params = dic
		# add entries of dic as attributes of this object
		self.__dict__.update(dic)
	
	def get(self,field):
		"""Returns value of specified field. The result is same as using the . operator to get value of the attribute."""
		return self.params[field]

	def get_dict(self):
		"""Returns all fields and their values as a dictionary"""
		return self.params
		
	def dump(self):
		"""Prints all key-value pairs of this object"""
		for k,v in self.params.iteritems():
			print k," : ",v